export function Glass(props: React.HTMLAttributes<HTMLDivElement>) {
  const { className = "", ...rest } = props as any;
  return <div {...rest} className={`glass-panel ${className}`} />;

export const __SYMBOL_TOOL_TEST__Glass = true;

export const __SYMBOL_TOOL_TEST__Glass = true;
}
